---
import { site } from '../data/resume';

const links = [
  { href: '#about', label: 'about' },
  { href: '#experience', label: 'experience' },
  { href: '#projects', label: 'projects' },
  { href: '#skills', label: 'skills' },
  { href: '#education', label: 'education' },
  { href: '#contact', label: 'contact' },
];
---

<header class="nav-wrap">
  <nav class="container nav">
    <a href="#top" class="logo">
      <span class="prompt">~/</span>imad<span class="cursor">_</span>
    </a>

    <button class="menu-btn" id="menu-btn" aria-label="Toggle menu" aria-expanded="false">
      <span class="mp">~/</span><span class="mt">menu</span>
    </button>

    <ul class="links" id="nav-links">
      {
        links.map((l, i) => (
          <li>
            <a href={l.href}>
              <span class="num">0{i + 1}.</span>
              {l.label}
            </a>
          </li>
        ))
      }
      <li>
        <a href={site.cvUrl} class="cv-btn" target="_blank" rel="noopener"> resume.pdf </a>
      </li>
    </ul>
  </nav>
</header>

<style>
  .nav-wrap {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    background: rgba(10, 14, 19, 0.8);
    backdrop-filter: blur(12px);
    border-bottom: 1px solid var(--border);
  }

  .nav {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 64px;
  }

  .logo {
    font-family: var(--mono);
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--text);
  }

  .logo .prompt {
    color: var(--accent);
  }

  .cursor {
    color: var(--accent);
    animation: blink 1.1s step-end infinite;
  }

  @keyframes blink {
    50% {
      opacity: 0;
    }
  }

  .links {
    display: flex;
    align-items: center;
    gap: 28px;
    list-style: none;
  }

  .links a {
    font-family: var(--mono);
    font-size: 0.82rem;
    color: var(--text-dim);
    transition: color 0.2s;
  }

  .links a:hover {
    color: var(--accent);
  }

  .num {
    color: var(--accent);
    margin-right: 4px;
  }

  .cv-btn {
    border: 1px solid var(--accent);
    border-radius: 6px;
    padding: 7px 14px;
    color: var(--accent) !important;
    transition:
      background 0.2s,
      transform 0.2s;
  }

  .cv-btn:hover {
    background: var(--accent-dim);
    transform: translateY(-1px);
  }

  .menu-btn {
    display: none;
    align-items: baseline;
    background: none;
    border: 1px solid var(--border);
    border-radius: 7px;
    cursor: pointer;
    padding: 8px 14px;
    font-family: var(--mono);
    font-size: 0.9rem;
    transition:
      border-color 0.2s,
      background 0.2s;
  }

  .menu-btn:hover,
  .menu-btn.open {
    border-color: var(--accent);
    background: var(--accent-dim);
  }

  .menu-btn .mp {
    color: var(--accent);
    font-weight: 600;
  }

  .menu-btn .mt {
    color: var(--text-dim);
  }

  .menu-btn.open .mt,
  .menu-btn:hover .mt {
    color: var(--text);
  }

  @media (max-width: 820px) {
    .menu-btn {
      display: flex;
    }

    .links {
      position: fixed;
      top: 64px;
      right: 0;
      flex-direction: column;
      align-items: flex-start;
      gap: 22px;
      background: var(--bg-elev);
      border: 1px solid var(--border);
      border-radius: 0 0 0 12px;
      padding: 28px 36px;
      transform: translateX(110%);
      transition: transform 0.3s ease;
    }

    .links.open {
      transform: translateX(0);
    }
  }
</style>

<script>
  const btn = document.getElementById('menu-btn');
  const links = document.getElementById('nav-links');
  btn?.addEventListener('click', () => {
    const open = links?.classList.toggle('open');
    btn.classList.toggle('open', open);
    btn.setAttribute('aria-expanded', String(open));
    const label = btn.querySelector('.mt');
    if (label) label.textContent = open ? 'close' : 'menu';
  });
  links?.querySelectorAll('a').forEach((a) =>
    a.addEventListener('click', () => {
      links.classList.remove('open');
      btn?.classList.remove('open');
      btn?.setAttribute('aria-expanded', 'false');
      const label = btn?.querySelector('.mt');
      if (label) label.textContent = 'menu';
    }),
  );
</script>
